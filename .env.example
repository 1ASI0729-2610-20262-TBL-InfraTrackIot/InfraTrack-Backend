# Local development
DATABASE_URL=localhost
DATABASE_PORT=3306
DATABASE_NAME=infratrack-os
DATABASE_USER=root
DATABASE_PASSWORD=password
PORT=8080

# Production (Render + filess.io MySQL) — set these in Render Environment, not in git
# DATABASE_URL=253cnj.h.filess.io
# DATABASE_PORT=3306
# DATABASE_NAME=db_infratrack_remarkable
# DATABASE_USER=db_infratrack_remarkable
# DATABASE_PASSWORD=your-password-here
# SPRING_PROFILES_ACTIVE=prod