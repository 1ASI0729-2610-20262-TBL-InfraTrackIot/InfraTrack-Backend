# Local development
DATABASE_HOST=localhost
DATABASE_PORT=3306
DATABASE_NAME=infratrack-os
DATABASE_USER=root
DATABASE_PASSWORD=password
PORT=8080

# Production (Render) — set at minimum DATABASE_PASSWORD in Render Environment
# DATABASE_HOST=253cnj.h.filess.io
# DATABASE_PORT=3306
# DATABASE_NAME=db_infratrack_remarkable
# DATABASE_USER=db_infratrack_remarkable
# DATABASE_PASSWORD=your-password-here
# SPRING_PROFILES_ACTIVE=prod
#
# Do NOT use DATABASE_URL on Render (Render may override it with another format)
